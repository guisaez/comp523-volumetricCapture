name: tests-client

on: 
  pull_request:
    paths:
      - 'files-service/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd client && npm install && npm run test