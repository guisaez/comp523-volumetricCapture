name: tests-client

on: 
  push:
    branches:
      - 'client-multiview'
    paths:
      - 'client/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd client && npm install && npm run test:ci